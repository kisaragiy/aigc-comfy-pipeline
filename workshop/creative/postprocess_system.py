#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
workshop/creative/postprocess_system.py — 后处理技术知识库 v1.0
=============================================================
AI 出图后处理（画师精修对应）——深挖第 9 轮
用法: 生成后按"超分→修脸→调色→锐化"顺序走——每步可选

【超分（放大质量——12G 卡注意显存）】
  方法对比:
    - ImageScaleBy（管线默认）: 快/轻——放大 2x 够用（无细节增强）
    - ESRGAN/Real-ESRGAN: 细节增强（放大+补细节——效果最好——但显存重）
    - 潜在空间超分（latent upscale）: 艺术感强（可能改画风）
  实战: 商业交付 2x 足够（9728x6656 从 4864 起）——4x 显存爆/收益低
  顺序: 先超分再修脸（修脸在高分辨率效果更好——今天已固化）

【修脸（face-detailer——12G 卡配置）】
  FaceDetailer（Impact Pack）: 检测人脸→局部重绘（修复崩脸/细节增强）
  参数: 脸够大自动跳过（"segment skip enough big"=正常）
  局限: 修脸会改肤色/风格（修完要 colorgrade 统一——顺序已固化）
  注意: 动漫脸可能检测不到（YOLO 误报/漏检——目视确认）

【调色（colorgrade——去脏/统一）】
  白平衡: 自动（灰世界假设）——去偏色（管线默认）
  色温: --warm 0.1 暖调 / -0.1 冷调（氛围控制）
  饱和度: 1.05 提鲜 / 0.9 高级灰（低饱和=电影感）
  强度: 0.5 保守（默认）——1.0 全量（画面明显偏色时）
  实战: 生成直出偏脏 → colorgrade 后"干净"（今天实测：白平衡 R0.84/G1.02/B1.21）

【锐化/降噪（终稿质感）】
  锐化: 边缘清晰（USM 少量——过度=锯齿）
  降噪: 高 ISO 感/噪点 → 轻微降噪（过度=糊）
  PIL: ImageFilter.SHARPEN / GaussianBlur 组合
  实战: 超分+colorgrade 后一般够——锐化只在"糊"时用

【特征后处理（泪痣/疤痕/纹身——模型画不准的特征）】
  泪痣: add_tear_mole.py（内眼角小点——用户规范）
  疤痕/纹身: 同理画（PIL 定位画——精确可控）
  原则: 模型画不准的"小特征"一律后处理画——不靠 prompt 赌

【后处理完整顺序（已固化——commercial_flow v1.1）】
  生成(含 face-detailer) → colorgrade → 泪痣(比例自适应) → 交付
  （超分在 create 内——泪痣画在超分后最终分辨率——比例自适应）

【实战决策表】
  图糊 → 超分 2x + 锐化
  脸崩 → face-detailer（重跑生成更快——修脸治标）
  色彩花/脏 → colorgrade（白平衡+降饱和）
  缺特征（泪痣） → add_tear_mole 后处理
  全部正常 → 直接交付
