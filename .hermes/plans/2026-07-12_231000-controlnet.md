# V0.23.0 — ControlNet 能力补齐 实现方案

> **Goal:** `python -m agents control "prompt" --ref ref.png --type depth` — ControlNet 引导生图，补齐市场要求的关键能力。
>
> **Architecture:** 新建 `agents/go_control.py` + ControlNet 工作流 JSON。用户已有 17 个 ControlNet 本地模型。

---

## 用户交互

```
# 深度控制（Depth）
python -m agents control "少女站在悬崖边" --ref depth_input.png --type depth

# 姿态控制（OpenPose）
python -m agents control "同一个姿势，赛博朋克风格" --ref pose.png --type openpose

# 软边缘控制（SoftEdge）
python -m agents control "线稿上色" --ref sketch.png --type softedge

# Tile 重绘
python -m agents control "增强细节" --ref lowres.png --type tile

# 预览模式
python -m agents control "prompt" --ref ref.png --type depth --dry-run
```

## ControlNet 工作流节点结构（SDXL）

```
CheckpointLoaderSimple(1)
  ├── model → ControlNetApply(6)[0]
  │             └── model → KSampler(7)[0]
  ├── clip → CLIPTextEncode(2)[0] → KSampler(7)[1] (positive)
  └── clip → CLIPTextEncode(3)[0] → KSampler(7)[2] (negative)

LoadImage(4) ──→ ControlNetLoader(5) ──→ ControlNetApply(6)[1] (controlnet)
                                        └── ControlNetApply(6)[2] (image)

EmptyLatentImage(8) ──→ KSampler(7)[3]
KSampler(7) ──→ VAEDecode(9) ──→ SaveImage(10)
```

## 类型 → 模型映射

| --type | ControlNet 模型 | 预处理 |
|--------|----------------|--------|
| depth | `controlnet-depth-sdxl-1.0.safetensors` | Depth 预处理器 |
| openpose | `OpenPoseXL2.safetensors` | OpenPose 预处理器 |
| softedge | `controlnet-sd-xl-1.0-softedge-dexined.safetensors` | SoftEdge 预处理器 |
| tile | `controlnet-tile-sdxl-1.0.safetensors` | Tile 预处理器 |
| inpaint | `controlnet_inpaint_sdxl1.safetensors` | Inpaint 预处理器 |
| lineart | `Kataragi_lineartXL-lora128.safetensors` | Lineart 预处理器 |

---

## 文件改动清单

| 操作 | 文件 | 说明 |
|------|------|------|
| Create | `agents/go_control.py` | ControlNet CLI |
| Create | `workflows/sdxl_controlnet_depth.json` | Depth 工作流模板 |
| Create | `workflows/sdxl_controlnet_openpose.json` | OpenPose 工作流模板 |
| Modify | `agents/__main__.py` | +`control` 子命令 |
| Modify | `AGENTS.md` | 版本 V0.23.0 |

---

## Task 1: 创建 go_control.py

**核心逻辑:**

```python
CONTROLNET_MODELS = {
    "depth": "controlnet-depth-sdxl-1.0.safetensors",
    "openpose": "OpenPoseXL2.safetensors",
    "softedge": "controlnet-sd-xl-1.0-softedge-dexined.safetensors",
    "tile": "controlnet-tile-sdxl-1.0.safetensors",
    "inpaint": "controlnet_inpaint_sdxl1.safetensors",
    "lineart": "Kataragi_lineartXL-lora128.safetensors",
}

def build_controlnet_workflow(
    prompt, ref_image, control_type="depth",
    negative="", seed=-1, steps=28, cfg=6.5,
    strength=0.8, width=1024, height=1024,
):
    """构建 SDXL + ControlNet 工作流。"""
    model = CONTROLNET_MODELS.get(control_type, CONTROLNET_MODELS["depth"])
    seed_actual = seed if seed != -1 else random.randint(1, 2**48 - 1)
    
    wf = {}
    wf["1"] = {"class_type": "CheckpointLoaderSimple", "inputs": {"ckpt_name": "sd_xl_base.safetensors"}}
    wf["2"] = {"class_type": "CLIPTextEncode", "inputs": {"text": prompt, "clip": ["1", 1]}}
    wf["3"] = {"class_type": "CLIPTextEncode", "inputs": {"text": negative or "worst quality, blurry", "clip": ["1", 1]}}
    wf["4"] = {"class_type": "LoadImage", "inputs": {"image": ref_image}}
    wf["5"] = {"class_type": "ControlNetLoader", "inputs": {"control_net_name": model}}
    wf["6"] = {"class_type": "ControlNetApply", "inputs": {
        "conditioning": ["2", 0], "control_net": ["5", 0],
        "image": ["4", 0], "strength": strength}}
    wf["7"] = {"class_type": "KSampler", "inputs": {"seed": seed_actual, "steps": steps, "cfg": cfg,
        "sampler_name": "dpmpp_2m", "scheduler": "karras", "denoise": 1.0,
        "model": ["1", 0], "positive": ["6", 0], "negative": ["3", 0],
        "latent_image": ["8", 0]}}
    wf["8"] = {"class_type": "EmptyLatentImage", "inputs": {"width": width, "height": height, "batch_size": 1}}
    wf["9"] = {"class_type": "VAEDecode", "inputs": {"samples": ["7", 0], "vae": ["1", 2]}}
    wf["10"] = {"class_type": "SaveImage", "inputs": {"images": ["9", 0], "filename_prefix": f"control_{control_type}"}}
    return wf, seed_actual
```

**验证:**
```bash
python -m agents control "prompt" --ref test.png --type depth --dry-run
```

---

## Task 2: workflow JSON 模板

通过 `scripts/build_workflows.py` 扩展，新增 ControlNet 工作流。

---

## Task 3: __main__.py + AGENTS.md

- +`control` 子命令
- 版本 V0.23.0

---

## 验证清单

- [ ] `python -m agents control "prompt" --ref ref.png --type depth --dry-run`
- [ ] `python -m agents control "prompt" --ref ref.png --type openpose --dry-run`
- [ ] 各 ControlNet 类型正确映射到模型文件
- [ ] `python -m agents --help` 显示 control 子命令
